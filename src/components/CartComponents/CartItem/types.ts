export type CartItemProps = {
  id: string;
  balls: number[];
  gameType: string;
  gamePrice: string;
  mainColor: string;
};

export type GameTypeTextProps = {
  colorText: string;
};

export type AfterImgTrashProps = {
  bgColor: string;
};
