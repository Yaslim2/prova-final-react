import { PageWrapper, NavBar } from "@UI/index";
import { AccountArea } from "@AccountComponents/index";

const Account: React.FC = () => {
    return <PageWrapper>
        <NavBar />
        <AccountArea />
    </PageWrapper>
}

export default Account;